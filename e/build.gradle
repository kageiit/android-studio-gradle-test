apply plugin: 'com.android.library'
apply plugin: 'com.neenbedankt.android-apt'

dependencies {
    compile project(':ze')
    compile deps.other.crittercism
    // compile deps.external.shuffle

    testCompile deps.test.robolectric
}
